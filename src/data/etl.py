def get_data(fb_id, outpath=None):
    '''
    Retrieve data for fb_id (facebook id) from the internet
    and return data (or write data to file, if `outpath` is
    not `None`).
    '''
    
    data = ...

    if outpath is None:
        return data
    else:
        ... # write data to outpath

        return data